<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Cuentas de Ahorro</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <h1>Cuentas de Ahorro</h1>
    <h2>Agregar Nueva Cuenta de Ahorro</h2>
    <form action="/cuentas/ahorros" method="post">
        <label for="usuarioId">ID del Usuario:</label>
        <input type="number" id="usuarioId" name="usuarioId" required>
        <label for="balance">Balance:</label>
        <input type="number" id="balance" name="balance" required>
        <label for="tasaInteres">Tasa de Interés:</label>
        <input type="number" step="0.01" id="tasaInteres" name="tasaInteres" required>
        <button type="submit">Agregar Cuenta</button>
    </form>

    <h2>Lista de Cuentas de Ahorro</h2>
    <ul>
        <% cuentas.forEach(function(cuenta) { if (cuenta.tipo === 'ahorro') { %>
            <li>
                ID: <%= cuenta.id %> - Usuario ID: <%= cuenta.usuarioId %> - Balance: <%= cuenta.balance %> - Tasa de Interés: <%= cuenta.tasaInteres %>
                <a href="/cuentas/ahorros/<%= cuenta.id %>/editar">Editar</a>
                <form action="/cuentas/ahorros/<%= cuenta.id %>?_method=DELETE" method="post" style="display:inline;">
                    <button type="submit">Eliminar</button>
                </form>
            </li>
        <% } }) %>
    </ul>
</body>
</html>
